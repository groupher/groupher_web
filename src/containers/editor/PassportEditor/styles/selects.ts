import styled from 'styled-components'

import css from '@/css'

export const Wrapper = styled.div`
  width: 100%;
  ${css.flex()};
  flex-wrap: wrap;
  gap: 10px 0;
`
export const Item = styled.div`
  width: 50%;
`
