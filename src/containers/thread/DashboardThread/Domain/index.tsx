import { FC, memo } from 'react'

import { Wrapper } from '../styles/domain'

const Domain: FC = () => {
  return (
    <Wrapper>
      <div>Domain</div>
    </Wrapper>
  )
}

export default memo(Domain)
