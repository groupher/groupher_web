'use client'

import { observer } from 'mobx-react-lite'

import CMS from '@/containers//thread/DashboardThread/CMS'

const DashboardPostPage = () => {
  return <CMS />
}

export default observer(DashboardPostPage)
