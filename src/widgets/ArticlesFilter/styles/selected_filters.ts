import styled, { css } from '@/css'

export const Wrapper = styled.div`
  ${css.rowWrap()};

  margin-left: 15px;
  margin-top: -1px;
  max-width: 80%;
`
export const TagWrapper = styled.div`
  margin-bottom: 2px;
`
