/* * *
{{properCase name}}
* */ import React from 'react' import { buildLog } from '@/logger' import
import { observer } from 'mobx-react-lite'
from './logic' /* eslint-disable-next-line */ const log = buildLog('C:{{properCase
  name
}}') class
{{properCase name}} extends React.Component { componentDidMount() {
const
{{preCurly ''}}{{camelCase name}}
{{afterCurly ''}}
= this.props logic.init({{camelCase name}}) } componentWillUnmount() {
logic.uninit() } render() { return (
<div>
  <h2>{{properCase name}}!</h2>
  <div>impress me!</div>
</div>
)}} export default observer({{properCase name}})